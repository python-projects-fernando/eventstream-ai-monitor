[tool:pytest]
# Add the 'src' directory to the Python path so modules can be imported
pythonpath = src

# Optional: Specify test paths explicitly if needed, although 'tests' is usually found automatically
testpaths = tests

# Optional: Common flags you might want
addopts = -v --tb=short